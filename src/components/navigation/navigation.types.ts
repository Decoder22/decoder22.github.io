interface NavigationProps {
    // Base props if needed
  }
  
  interface DesktopNavigationProps {
    // Desktop specific props if needed
  }
  
  interface MobileNavigationProps {
    // Mobile specific props if needed
  }
  
  export type { NavigationProps, DesktopNavigationProps, MobileNavigationProps } 